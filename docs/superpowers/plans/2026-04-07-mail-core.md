# Mail Core Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build subsystem 1 of crm.eg.je — a Node/TS backend that aggregates IMAP from multiple Mail.ru mailboxes, sends via SMTP, persists messages/attachments in Postgres, and exposes a REST API ready for the web UI to consume.

**Architecture:** pnpm monorepo with `apps/api` (Fastify HTTP + workers) and `packages/db` + `packages/mail`. Postgres 16 via Prisma. BullMQ on Redis for delayed sends and the daily trash cleanup. Two systemd units (`crm-api`, `crm-sync`) behind nginx + certbot on `crm.eg.je`.

**Tech Stack:** Node 22, TypeScript (strict), Fastify, Prisma, Postgres 16, Redis, BullMQ, ImapFlow, Nodemailer, mailparser, argon2, Zod, Vitest, @testcontainers/postgresql, greenmail (docker), pino.

**Spec:** `docs/superpowers/specs/2026-04-07-mail-core-design.md`

---

## File Structure

```
/opt/crm.eg.je/
├── package.json                       # workspace root
├── pnpm-workspace.yaml
├── tsconfig.base.json
├── .gitignore
├── .env.example
├── apps/
│   └── api/
│       ├── package.json
│       ├── tsconfig.json
│       ├── vitest.config.ts
│       ├── src/
│       │   ├── server.ts              # fastify bootstrap (HTTP entrypoint)
│       │   ├── workers.ts             # sync + scheduled-send entrypoint
│       │   ├── config.ts              # env loader (zod)
│       │   ├── crypto.ts              # AES-256-GCM helpers
│       │   ├── auth.ts                # session + argon2 + guards
│       │   ├── errors.ts              # typed errors + fastify error handler
│       │   ├── queue.ts               # BullMQ singletons
│       │   ├── routes/
│       │   │   ├── auth.ts
│       │   │   ├── mailboxes.ts
│       │   │   ├── folders.ts
│       │   │   ├── messages.ts
│       │   │   └── attachments.ts
│       │   ├── services/
│       │   │   ├── messages.ts        # business logic (draft, move, soft-delete)
│       │   │   ├── send.ts            # internal send function (used by route + worker)
│       │   │   └── search.ts          # postgres FTS query builder
│       │   ├── workers/
│       │   │   ├── sync.ts            # IMAP IDLE per mailbox
│       │   │   ├── scheduled-send.ts  # BullMQ consumer
│       │   │   └── trash-cleanup.ts   # daily repeatable job
│       │   └── cli/
│       │       ├── seed-owner.ts
│       │       └── seed-mailbox.ts
│       └── test/
│           ├── helpers/
│           │   ├── containers.ts      # testcontainers wiring
│           │   └── greenmail.ts
│           ├── unit/
│           │   ├── crypto.test.ts
│           │   ├── search.test.ts
│           │   └── send.test.ts
│           └── integration/
│               ├── auth.test.ts
│               ├── folders.test.ts
│               ├── messages.test.ts
│               ├── send-receive.test.ts
│               ├── scheduled-send.test.ts
│               └── trash-cleanup.test.ts
├── packages/
│   ├── db/
│   │   ├── package.json
│   │   ├── prisma/schema.prisma
│   │   └── src/index.ts               # PrismaClient singleton export
│   └── mail/
│       ├── package.json
│       ├── src/
│       │   ├── imap.ts                # ImapFlow wrapper
│       │   ├── smtp.ts                # Nodemailer wrapper
│       │   └── parse.ts               # mailparser wrapper
│       └── test/
│           └── parse.test.ts
└── ops/
    ├── systemd/
    │   ├── crm-api.service
    │   └── crm-sync.service
    ├── nginx/crm.eg.je.conf
    └── README.md                      # deploy runbook
```

---

## Task 1: Workspace scaffold

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.base.json`
- Create: `.gitignore`
- Create: `.env.example`

- [ ] **Step 1: Create workspace root files**

`package.json`:
```json
{
  "name": "crm.eg.je",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev:api": "pnpm --filter @crm/api dev",
    "dev:workers": "pnpm --filter @crm/api workers"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
```

`pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "outDir": "dist"
  }
}
```

`.gitignore`:
```
node_modules
dist
*.log
.env
/var
```

`.env.example`:
```
DATABASE_URL=postgresql://crm:crm@localhost:5432/crm
REDIS_URL=redis://localhost:6379
CRM_ENC_KEY=base64-32-bytes-here
SESSION_SECRET=base64-32-bytes-here
ATTACHMENT_DIR=/var/lib/crm/attachments
PORT=3000
NODE_ENV=development
```

- [ ] **Step 2: Install pnpm if missing and run install**

```bash
corepack enable
corepack prepare pnpm@9.12.0 --activate
cd /opt/crm.eg.je && pnpm install
```
Expected: lockfile created, no errors.

- [ ] **Step 3: Commit**

```bash
git add . && git commit -m "chore: workspace scaffold"
```

---

## Task 2: db package + Prisma schema

**Files:**
- Create: `packages/db/package.json`
- Create: `packages/db/prisma/schema.prisma`
- Create: `packages/db/src/index.ts`
- Create: `packages/db/tsconfig.json`

- [ ] **Step 1: Create package.json**

```json
{
  "name": "@crm/db",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
```

- [ ] **Step 2: Write schema.prisma**

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role { owner admin manager }
enum FolderKind { inbox sent drafts trash custom }
enum SendStatus { pending sent failed cancelled }

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String
  role         Role
  createdAt    DateTime @default(now())
  lastLoginAt  DateTime?
  folders      Folder[]
  scheduled    ScheduledSend[]
}

model Mailbox {
  id                   String   @id @default(cuid())
  email                String   @unique
  displayName          String
  encryptedAppPassword Bytes
  imapHost             String   @default("imap.mail.ru")
  imapPort             Int      @default(993)
  smtpHost             String   @default("smtp.mail.ru")
  smtpPort             Int      @default(465)
  enabled              Boolean  @default(true)
  createdAt            DateTime @default(now())
  folders              Folder[]
  messages             Message[]
  scheduled            ScheduledSend[]
  syncState            SyncState?
}

model SyncState {
  mailboxId String  @id
  mailbox   Mailbox @relation(fields: [mailboxId], references: [id], onDelete: Cascade)
  lastUid   Int     @default(0)
  updatedAt DateTime @updatedAt
}

model Folder {
  id        String     @id @default(cuid())
  mailbox   Mailbox?   @relation(fields: [mailboxId], references: [id])
  mailboxId String?
  owner     User?      @relation(fields: [ownerId], references: [id])
  ownerId   String?
  name      String
  kind      FolderKind
  parentId  String?
  messages  Message[]
  @@index([mailboxId, kind])
}

model Message {
  id          String   @id @default(cuid())
  mailbox     Mailbox  @relation(fields: [mailboxId], references: [id])
  mailboxId   String
  folder      Folder   @relation(fields: [folderId], references: [id])
  folderId    String
  imapUid     Int?
  messageId   String?  @unique
  fromAddr    String
  toAddrs     String[]
  ccAddrs     String[]
  subject     String
  bodyText    String?
  bodyHtml    String?
  isRead      Boolean  @default(false)
  isStarred   Boolean  @default(false)
  isDraft     Boolean  @default(false)
  receivedAt  DateTime?
  sentAt      DateTime?
  deletedAt   DateTime?
  attachments Attachment[]
  @@index([mailboxId, folderId, receivedAt])
  @@index([deletedAt])
}

model Attachment {
  id          String  @id @default(cuid())
  message     Message @relation(fields: [messageId], references: [id], onDelete: Cascade)
  messageId   String
  filename    String
  mime        String
  size        Int
  storagePath String
}

model ScheduledSend {
  id        String     @id @default(cuid())
  user      User       @relation(fields: [userId], references: [id])
  userId    String
  mailbox   Mailbox    @relation(fields: [mailboxId], references: [id])
  mailboxId String
  payload   Json
  sendAt    DateTime
  status    SendStatus @default(pending)
  jobId     String?
  createdAt DateTime   @default(now())
}
```

- [ ] **Step 3: Create client export**

`packages/db/src/index.ts`:
```ts
import { PrismaClient } from "@prisma/client";
export const prisma = new PrismaClient();
export * from "@prisma/client";
```

`packages/db/tsconfig.json`:
```json
{ "extends": "../../tsconfig.base.json", "include": ["src"] }
```

- [ ] **Step 4: Generate client and create initial migration**

```bash
cd /opt/crm.eg.je && pnpm install
# Make sure local Postgres has a crm db + role:
sudo -u postgres psql -c "CREATE ROLE crm LOGIN PASSWORD 'crm';" || true
sudo -u postgres psql -c "CREATE DATABASE crm OWNER crm;" || true
DATABASE_URL=postgresql://crm:crm@localhost:5432/crm \
  pnpm --filter @crm/db exec prisma migrate dev --name init
```
Expected: `migrations/<ts>_init/migration.sql` created, client generated.

- [ ] **Step 5: Add FTS column via raw migration**

Create `packages/db/prisma/migrations/<ts>_message_fts/migration.sql`:
```sql
ALTER TABLE "Message"
  ADD COLUMN "fts" tsvector GENERATED ALWAYS AS (
    to_tsvector('simple', coalesce("subject",'') || ' ' || coalesce("bodyText",''))
  ) STORED;
CREATE INDEX "Message_fts_idx" ON "Message" USING GIN ("fts");
```
Then:
```bash
DATABASE_URL=postgresql://crm:crm@localhost:5432/crm \
  pnpm --filter @crm/db exec prisma migrate dev
```
Expected: migration applied.

- [ ] **Step 6: Commit**

```bash
git add . && git commit -m "feat(db): initial schema + fts index"
```

---

## Task 3: Crypto helpers (TDD)

**Files:**
- Create: `apps/api/package.json`
- Create: `apps/api/tsconfig.json`
- Create: `apps/api/vitest.config.ts`
- Create: `apps/api/src/crypto.ts`
- Create: `apps/api/test/unit/crypto.test.ts`

- [ ] **Step 1: Create apps/api package skeleton**

`apps/api/package.json`:
```json
{
  "name": "@crm/api",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "workers": "tsx watch src/workers.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "start:workers": "node dist/workers.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@crm/db": "workspace:*",
    "@fastify/multipart": "^9.0.1",
    "@fastify/secure-session": "^8.1.0",
    "argon2": "^0.41.1",
    "bullmq": "^5.21.0",
    "fastify": "^5.1.0",
    "imapflow": "^1.0.171",
    "mailparser": "^3.7.2",
    "nodemailer": "^6.9.16",
    "pino": "^9.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^10.13.2",
    "@types/node": "^22.9.0",
    "@types/nodemailer": "^6.4.16",
    "testcontainers": "^10.13.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
```

`apps/api/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```

`apps/api/vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";
export default defineConfig({
  test: {
    include: ["test/**/*.test.ts"],
    testTimeout: 60_000,
    hookTimeout: 120_000,
  },
});
```

```bash
pnpm install
```

- [ ] **Step 2: Write failing crypto test**

`apps/api/test/unit/crypto.test.ts`:
```ts
import { describe, it, expect, beforeAll } from "vitest";
import { encrypt, decrypt, setKey } from "../../src/crypto.js";
import { randomBytes } from "node:crypto";

beforeAll(() => setKey(randomBytes(32)));

describe("crypto", () => {
  it("round-trips a string", () => {
    const blob = encrypt("hunter2", "user@mail.ru");
    expect(decrypt(blob, "user@mail.ru")).toBe("hunter2");
  });

  it("rejects wrong AAD", () => {
    const blob = encrypt("hunter2", "user@mail.ru");
    expect(() => decrypt(blob, "evil@mail.ru")).toThrow();
  });

  it("produces different ciphertexts for same plaintext", () => {
    const a = encrypt("x", "u@m");
    const b = encrypt("x", "u@m");
    expect(a.equals(b)).toBe(false);
  });
});
```

- [ ] **Step 3: Run, verify failure**

```bash
pnpm --filter @crm/api test crypto
```
Expected: FAIL — module not found.

- [ ] **Step 4: Implement crypto.ts**

```ts
import { createCipheriv, createDecipheriv, randomBytes } from "node:crypto";

let KEY: Buffer | null = null;
export function setKey(k: Buffer) {
  if (k.length !== 32) throw new Error("CRM_ENC_KEY must be 32 bytes");
  KEY = k;
}
function key(): Buffer {
  if (!KEY) throw new Error("encryption key not set");
  return KEY;
}

export function encrypt(plaintext: string, aad: string): Buffer {
  const iv = randomBytes(12);
  const cipher = createCipheriv("aes-256-gcm", key(), iv);
  cipher.setAAD(Buffer.from(aad));
  const ct = Buffer.concat([cipher.update(plaintext, "utf8"), cipher.final()]);
  const tag = cipher.getAuthTag();
  return Buffer.concat([iv, ct, tag]);
}

export function decrypt(blob: Buffer, aad: string): string {
  const iv = blob.subarray(0, 12);
  const tag = blob.subarray(blob.length - 16);
  const ct = blob.subarray(12, blob.length - 16);
  const decipher = createDecipheriv("aes-256-gcm", key(), iv);
  decipher.setAAD(Buffer.from(aad));
  decipher.setAuthTag(tag);
  return Buffer.concat([decipher.update(ct), decipher.final()]).toString("utf8");
}
```

- [ ] **Step 5: Run, verify pass**

```bash
pnpm --filter @crm/api test crypto
```
Expected: 3 passing.

- [ ] **Step 6: Commit**

```bash
git add . && git commit -m "feat(api): aes-256-gcm crypto helpers"
```

---

## Task 4: Config loader

**Files:**
- Create: `apps/api/src/config.ts`
- Create: `apps/api/test/unit/config.test.ts`

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect } from "vitest";
import { loadConfig } from "../../src/config.js";

describe("config", () => {
  it("parses env into typed config", () => {
    const cfg = loadConfig({
      DATABASE_URL: "postgresql://x",
      REDIS_URL: "redis://x",
      CRM_ENC_KEY: Buffer.alloc(32).toString("base64"),
      SESSION_SECRET: Buffer.alloc(32).toString("base64"),
      ATTACHMENT_DIR: "/tmp/a",
      PORT: "3001",
      NODE_ENV: "test",
    });
    expect(cfg.port).toBe(3001);
    expect(cfg.encKey.length).toBe(32);
  });

  it("rejects short enc key", () => {
    expect(() => loadConfig({
      DATABASE_URL: "x", REDIS_URL: "x",
      CRM_ENC_KEY: "short",
      SESSION_SECRET: Buffer.alloc(32).toString("base64"),
      ATTACHMENT_DIR: "/tmp/a", PORT: "1", NODE_ENV: "test",
    })).toThrow();
  });
});
```

- [ ] **Step 2: Run, verify failure**

```bash
pnpm --filter @crm/api test config
```

- [ ] **Step 3: Implement config.ts**

```ts
import { z } from "zod";

const Schema = z.object({
  DATABASE_URL: z.string().min(1),
  REDIS_URL: z.string().min(1),
  CRM_ENC_KEY: z.string().min(1),
  SESSION_SECRET: z.string().min(1),
  ATTACHMENT_DIR: z.string().min(1),
  PORT: z.coerce.number().int().positive(),
  NODE_ENV: z.enum(["development", "test", "production"]),
});

export type Config = {
  databaseUrl: string;
  redisUrl: string;
  encKey: Buffer;
  sessionSecret: Buffer;
  attachmentDir: string;
  port: number;
  env: "development" | "test" | "production";
};

export function loadConfig(env: NodeJS.ProcessEnv = process.env): Config {
  const p = Schema.parse(env);
  const encKey = Buffer.from(p.CRM_ENC_KEY, "base64");
  if (encKey.length !== 32) throw new Error("CRM_ENC_KEY must be base64(32)");
  const sessionSecret = Buffer.from(p.SESSION_SECRET, "base64");
  if (sessionSecret.length !== 32) throw new Error("SESSION_SECRET must be base64(32)");
  return {
    databaseUrl: p.DATABASE_URL,
    redisUrl: p.REDIS_URL,
    encKey,
    sessionSecret,
    attachmentDir: p.ATTACHMENT_DIR,
    port: p.PORT,
    env: p.NODE_ENV,
  };
}
```

- [ ] **Step 4: Run, verify pass**

- [ ] **Step 5: Commit**

```bash
git add . && git commit -m "feat(api): typed config loader"
```

---

## Task 5: Errors + Fastify bootstrap

**Files:**
- Create: `apps/api/src/errors.ts`
- Create: `apps/api/src/server.ts`

- [ ] **Step 1: errors.ts**

```ts
import type { FastifyInstance } from "fastify";
import { ZodError } from "zod";

export class HttpError extends Error {
  constructor(public status: number, message: string, public details?: unknown) {
    super(message);
  }
}
export class NotFound extends HttpError { constructor(m = "not found") { super(404, m); } }
export class Forbidden extends HttpError { constructor(m = "forbidden") { super(403, m); } }
export class BadRequest extends HttpError { constructor(m: string, d?: unknown) { super(400, m, d); } }

export function registerErrorHandler(app: FastifyInstance) {
  app.setErrorHandler((err, req, reply) => {
    if (err instanceof ZodError) {
      return reply.status(400).send({ error: "validation", details: err.flatten() });
    }
    if (err instanceof HttpError) {
      return reply.status(err.status).send({ error: err.message, details: err.details });
    }
    req.log.error({ err }, "unhandled");
    return reply.status(500).send({ error: "internal", requestId: req.id });
  });
}
```

- [ ] **Step 2: server.ts (HTTP entrypoint, no routes yet)**

```ts
import Fastify from "fastify";
import secureSession from "@fastify/secure-session";
import multipart from "@fastify/multipart";
import { loadConfig } from "./config.js";
import { setKey } from "./crypto.js";
import { registerErrorHandler } from "./errors.js";

export async function buildApp() {
  const cfg = loadConfig();
  setKey(cfg.encKey);

  const app = Fastify({ logger: { level: cfg.env === "test" ? "warn" : "info" } });
  await app.register(secureSession, {
    key: cfg.sessionSecret,
    cookie: { path: "/", httpOnly: true, sameSite: "lax", secure: cfg.env === "production" },
  });
  await app.register(multipart, { limits: { fileSize: 25 * 1024 * 1024 } });

  registerErrorHandler(app);

  app.get("/health", async () => ({ ok: true }));

  return { app, cfg };
}

if (import.meta.url === `file://${process.argv[1]}`) {
  const { app, cfg } = await buildApp();
  await app.listen({ host: "127.0.0.1", port: cfg.port });
}
```

- [ ] **Step 3: Smoke check**

```bash
pnpm --filter @crm/api build
```
Expected: clean tsc.

- [ ] **Step 4: Commit**

```bash
git add . && git commit -m "feat(api): fastify bootstrap + error handler"
```

---

## Task 6: Auth (argon2 + session + login/logout/me)

**Files:**
- Create: `apps/api/src/auth.ts`
- Create: `apps/api/src/routes/auth.ts`
- Create: `apps/api/test/integration/auth.test.ts`
- Create: `apps/api/test/helpers/containers.ts`
- Modify: `apps/api/src/server.ts` (register routes)

- [ ] **Step 1: helpers/containers.ts**

```ts
import { PostgreSqlContainer, StartedPostgreSqlContainer } from "@testcontainers/postgresql";
import { GenericContainer, StartedTestContainer } from "testcontainers";
import { execSync } from "node:child_process";
import { randomBytes } from "node:crypto";

export type Stack = {
  pg: StartedPostgreSqlContainer;
  redis: StartedTestContainer;
  databaseUrl: string;
  redisUrl: string;
  stop: () => Promise<void>;
};

export async function startStack(): Promise<Stack> {
  const pg = await new PostgreSqlContainer("postgres:16").start();
  const redis = await new GenericContainer("redis:7").withExposedPorts(6379).start();
  const databaseUrl = pg.getConnectionUri();
  const redisUrl = `redis://${redis.getHost()}:${redis.getMappedPort(6379)}`;
  execSync("pnpm --filter @crm/db exec prisma migrate deploy", {
    env: { ...process.env, DATABASE_URL: databaseUrl },
    stdio: "inherit",
  });
  return { pg, redis, databaseUrl, redisUrl, stop: async () => { await redis.stop(); await pg.stop(); } };
}

export function testEnv(databaseUrl: string, redisUrl: string, attachmentDir: string) {
  return {
    DATABASE_URL: databaseUrl,
    REDIS_URL: redisUrl,
    CRM_ENC_KEY: randomBytes(32).toString("base64"),
    SESSION_SECRET: randomBytes(32).toString("base64"),
    ATTACHMENT_DIR: attachmentDir,
    PORT: "0",
    NODE_ENV: "test" as const,
  };
}
```

- [ ] **Step 2: Failing auth test**

```ts
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { startStack, testEnv, type Stack } from "../helpers/containers.js";
import { buildApp } from "../../src/server.js";
import { prisma } from "@crm/db";
import argon2 from "argon2";

let stack: Stack;
let app: Awaited<ReturnType<typeof buildApp>>["app"];

beforeAll(async () => {
  stack = await startStack();
  Object.assign(process.env, testEnv(stack.databaseUrl, stack.redisUrl, mkdtempSync(join(tmpdir(), "crm-"))));
  ({ app } = await buildApp());
  await prisma.user.create({
    data: { email: "owner@x", passwordHash: await argon2.hash("pw"), name: "Owner", role: "owner" },
  });
}, 180_000);

afterAll(async () => { await app.close(); await prisma.$disconnect(); await stack.stop(); });

describe("auth", () => {
  it("rejects bad password", async () => {
    const r = await app.inject({ method: "POST", url: "/auth/login", payload: { email: "owner@x", password: "no" } });
    expect(r.statusCode).toBe(401);
  });

  it("logs in and returns me", async () => {
    const login = await app.inject({ method: "POST", url: "/auth/login", payload: { email: "owner@x", password: "pw" } });
    expect(login.statusCode).toBe(200);
    const cookie = login.headers["set-cookie"]!;
    const me = await app.inject({ method: "GET", url: "/me", headers: { cookie: String(cookie) } });
    expect(me.statusCode).toBe(200);
    expect(me.json().email).toBe("owner@x");
  });

  it("/me without session is 401", async () => {
    const r = await app.inject({ method: "GET", url: "/me" });
    expect(r.statusCode).toBe(401);
  });
});
```

- [ ] **Step 3: Run, verify failure**

```bash
pnpm --filter @crm/api test auth
```
Expected: FAIL (routes don't exist).

- [ ] **Step 4: auth.ts (helpers)**

```ts
import argon2 from "argon2";
import type { FastifyRequest } from "fastify";
import { Forbidden } from "./errors.js";
import { prisma, Role } from "@crm/db";

export async function hashPassword(p: string) { return argon2.hash(p, { type: argon2.argon2id }); }
export async function verifyPassword(hash: string, p: string) { return argon2.verify(hash, p); }

export async function currentUser(req: FastifyRequest) {
  const id = req.session.get("uid");
  if (!id) return null;
  return prisma.user.findUnique({ where: { id } });
}

export function requireUser() {
  return async (req: FastifyRequest) => {
    const u = await currentUser(req);
    if (!u) throw new Forbidden("not authenticated");
    (req as any).user = u;
  };
}

export function requireRole(...roles: Role[]) {
  return async (req: FastifyRequest) => {
    const u = await currentUser(req);
    if (!u) throw new Forbidden("not authenticated");
    if (!roles.includes(u.role)) throw new Forbidden("insufficient role");
    (req as any).user = u;
  };
}
```

- [ ] **Step 5: routes/auth.ts**

```ts
import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { prisma } from "@crm/db";
import { verifyPassword, currentUser } from "../auth.js";

const LoginBody = z.object({ email: z.string().email(), password: z.string().min(1) });

export async function authRoutes(app: FastifyInstance) {
  app.post("/auth/login", async (req, reply) => {
    const body = LoginBody.parse(req.body);
    const u = await prisma.user.findUnique({ where: { email: body.email } });
    if (!u || !(await verifyPassword(u.passwordHash, body.password))) {
      return reply.status(401).send({ error: "invalid credentials" });
    }
    req.session.set("uid", u.id);
    await prisma.user.update({ where: { id: u.id }, data: { lastLoginAt: new Date() } });
    return { id: u.id, email: u.email, name: u.name, role: u.role };
  });

  app.post("/auth/logout", async (req) => { req.session.delete(); return { ok: true }; });

  app.get("/me", async (req, reply) => {
    const u = await currentUser(req);
    if (!u) return reply.status(401).send({ error: "not authenticated" });
    return { id: u.id, email: u.email, name: u.name, role: u.role };
  });
}
```

- [ ] **Step 6: Register in server.ts**

Add to `buildApp` after multipart:
```ts
import { authRoutes } from "./routes/auth.js";
// ...
await app.register(authRoutes);
```

- [ ] **Step 7: Run, verify pass**

```bash
pnpm --filter @crm/api test auth
```
Expected: 3 passing.

- [ ] **Step 8: Commit**

```bash
git add . && git commit -m "feat(api): auth routes (login/logout/me)"
```

---

## Task 7: Mailbox + folder routes (read-side)

**Files:**
- Create: `apps/api/src/routes/mailboxes.ts`
- Create: `apps/api/src/routes/folders.ts`
- Create: `apps/api/test/integration/folders.test.ts`
- Modify: `apps/api/src/server.ts`

- [ ] **Step 1: Failing folder test**

```ts
// folders.test.ts — login as seeded owner, then:
// - GET /folders → empty list
// - POST /folders { name: "Clients" } → 200, returns id
// - GET /folders → contains "Clients"
// - PATCH /folders/:id { name: "VIP" } → renamed
// - DELETE /folders/:id → 204
```
Write the actual code mirroring `auth.test.ts` setup, with login helper:
```ts
async function login() {
  const r = await app.inject({ method: "POST", url: "/auth/login", payload: { email: "owner@x", password: "pw" } });
  return String(r.headers["set-cookie"]);
}
```
Then the four assertions above.

- [ ] **Step 2: Run, verify failure**

- [ ] **Step 3: Implement mailboxes.ts**

```ts
import type { FastifyInstance } from "fastify";
import { prisma } from "@crm/db";
import { requireUser } from "../auth.js";

export async function mailboxRoutes(app: FastifyInstance) {
  app.get("/mailboxes", { preHandler: requireUser() }, async () => {
    return prisma.mailbox.findMany({
      where: { enabled: true },
      select: { id: true, email: true, displayName: true, enabled: true },
      orderBy: { displayName: "asc" },
    });
  });
}
```

- [ ] **Step 4: Implement folders.ts**

```ts
import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { prisma } from "@crm/db";
import { requireUser } from "../auth.js";
import { NotFound } from "../errors.js";

const Create = z.object({ name: z.string().min(1).max(80), mailboxId: z.string().optional() });
const Patch = z.object({ name: z.string().min(1).max(80) });

export async function folderRoutes(app: FastifyInstance) {
  app.get("/folders", { preHandler: requireUser() }, async (req) => {
    const user = (req as any).user;
    return prisma.folder.findMany({
      where: { OR: [{ ownerId: user.id }, { mailboxId: { not: null } }] },
      orderBy: [{ kind: "asc" }, { name: "asc" }],
    });
  });

  app.post("/folders", { preHandler: requireUser() }, async (req) => {
    const body = Create.parse(req.body);
    const user = (req as any).user;
    return prisma.folder.create({
      data: { name: body.name, kind: "custom", ownerId: user.id, mailboxId: body.mailboxId ?? null },
    });
  });

  app.patch("/folders/:id", { preHandler: requireUser() }, async (req) => {
    const id = (req.params as any).id as string;
    const body = Patch.parse(req.body);
    const f = await prisma.folder.findUnique({ where: { id } });
    if (!f || f.kind !== "custom") throw new NotFound("folder not found");
    return prisma.folder.update({ where: { id }, data: { name: body.name } });
  });

  app.delete("/folders/:id", { preHandler: requireUser() }, async (req, reply) => {
    const id = (req.params as any).id as string;
    const f = await prisma.folder.findUnique({ where: { id }, include: { messages: true } });
    if (!f || f.kind !== "custom") throw new NotFound("folder not found");
    if (f.messages.length) throw new NotFound("folder not empty");
    await prisma.folder.delete({ where: { id } });
    return reply.status(204).send();
  });
}
```

- [ ] **Step 5: Register routes; run; pass**

In `server.ts`:
```ts
import { mailboxRoutes } from "./routes/mailboxes.js";
import { folderRoutes } from "./routes/folders.js";
await app.register(mailboxRoutes);
await app.register(folderRoutes);
```

```bash
pnpm --filter @crm/api test folders
```
Expected: passing.

- [ ] **Step 6: Commit**

```bash
git add . && git commit -m "feat(api): mailbox + folder routes"
```

---

## Task 8: Mail wrappers (packages/mail)

**Files:**
- Create: `packages/mail/package.json`
- Create: `packages/mail/src/imap.ts`
- Create: `packages/mail/src/smtp.ts`
- Create: `packages/mail/src/parse.ts`
- Create: `packages/mail/test/parse.test.ts`
- Create: `packages/mail/tsconfig.json`

- [ ] **Step 1: package.json + tsconfig**

```json
{
  "name": "@crm/mail",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "dependencies": {
    "imapflow": "^1.0.171",
    "mailparser": "^3.7.2",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.5",
    "@types/nodemailer": "^6.4.16",
    "vitest": "^2.1.5"
  }
}
```
```json
{ "extends": "../../tsconfig.base.json", "include": ["src"] }
```

- [ ] **Step 2: parse.ts + failing test**

```ts
// parse.ts
import { simpleParser, ParsedMail } from "mailparser";
export type Parsed = {
  messageId?: string;
  from: string;
  to: string[];
  cc: string[];
  subject: string;
  text?: string;
  html?: string;
  date?: Date;
  attachments: { filename: string; mime: string; size: number; content: Buffer }[];
};
export async function parseRaw(raw: Buffer): Promise<Parsed> {
  const m: ParsedMail = await simpleParser(raw);
  const addrs = (a: any) => (Array.isArray(a) ? a : a ? [a] : []).flatMap((x: any) => x.value.map((v: any) => v.address!));
  return {
    messageId: m.messageId,
    from: m.from?.value[0]?.address ?? "",
    to: addrs(m.to),
    cc: addrs(m.cc),
    subject: m.subject ?? "",
    text: m.text ?? undefined,
    html: typeof m.html === "string" ? m.html : undefined,
    date: m.date,
    attachments: (m.attachments ?? []).map(a => ({
      filename: a.filename ?? "file",
      mime: a.contentType,
      size: a.size,
      content: a.content,
    })),
  };
}
```

`test/parse.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { parseRaw } from "../src/parse.js";

const RAW = Buffer.from(
  [
    "From: a@x",
    "To: b@y",
    "Subject: hi",
    "Message-Id: <1@x>",
    "Content-Type: text/plain",
    "",
    "hello",
    "",
  ].join("\r\n"),
);

describe("parseRaw", () => {
  it("extracts headers + body", async () => {
    const p = await parseRaw(RAW);
    expect(p.from).toBe("a@x");
    expect(p.to).toEqual(["b@y"]);
    expect(p.subject).toBe("hi");
    expect(p.text?.trim()).toBe("hello");
    expect(p.messageId).toBe("<1@x>");
  });
});
```

```bash
pnpm install
pnpm --filter @crm/mail test
```
Expected: passing.

- [ ] **Step 3: smtp.ts**

```ts
import nodemailer from "nodemailer";
export type SendInput = {
  host: string; port: number; user: string; pass: string;
  from: string; to: string[]; cc?: string[]; subject: string;
  text?: string; html?: string;
  attachments?: { filename: string; content: Buffer; contentType: string }[];
};
export async function sendMail(i: SendInput) {
  const t = nodemailer.createTransport({
    host: i.host, port: i.port, secure: i.port === 465,
    auth: { user: i.user, pass: i.pass },
  });
  return t.sendMail({
    from: i.from, to: i.to, cc: i.cc, subject: i.subject,
    text: i.text, html: i.html, attachments: i.attachments,
  });
}
```

- [ ] **Step 4: imap.ts (thin wrapper)**

```ts
import { ImapFlow } from "imapflow";

export type ImapCreds = { host: string; port: number; user: string; pass: string };

export function newClient(c: ImapCreds) {
  return new ImapFlow({
    host: c.host, port: c.port, secure: true,
    auth: { user: c.user, pass: c.pass },
    logger: false,
  });
}

export async function appendToSent(c: ImapCreds, raw: Buffer) {
  const client = newClient(c);
  await client.connect();
  try {
    await client.append("Sent", raw, ["\\Seen"]);
  } finally {
    await client.logout();
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add . && git commit -m "feat(mail): imap/smtp/parse wrappers"
```

---

## Task 9: Send service + send route

**Files:**
- Create: `apps/api/src/services/send.ts`
- Modify: `apps/api/src/routes/messages.ts` (created in this task)
- Create: `apps/api/test/unit/send.test.ts`

- [ ] **Step 1: Failing unit test (mock SMTP)**

```ts
import { describe, it, expect, vi } from "vitest";
vi.mock("@crm/mail", () => ({
  sendMail: vi.fn().mockResolvedValue({ messageId: "<m@x>" }),
  appendToSent: vi.fn().mockResolvedValue(undefined),
}));
import { sendMessage } from "../../src/services/send.js";

describe("sendMessage", () => {
  it("calls smtp with decrypted password", async () => {
    const mailbox = {
      id: "mb1", email: "from@x", displayName: "x",
      encryptedAppPassword: Buffer.alloc(0), // crypto bypassed via injected decryptor
      smtpHost: "smtp", smtpPort: 465, imapHost: "imap", imapPort: 993, enabled: true,
    } as any;
    const out = await sendMessage(
      { mailbox, decrypt: () => "secret" },
      { from: "from@x", to: ["to@y"], subject: "s", text: "t" },
    );
    expect(out.messageId).toBe("<m@x>");
  });
});
```

- [ ] **Step 2: Run, fail, then implement services/send.ts**

```ts
import { sendMail, appendToSent } from "@crm/mail";
import type { Mailbox } from "@crm/db";

export type SendCtx = { mailbox: Mailbox; decrypt: (b: Buffer) => string };
export type SendPayload = {
  from: string; to: string[]; cc?: string[];
  subject: string; text?: string; html?: string;
  attachments?: { filename: string; content: Buffer; contentType: string }[];
};

export async function sendMessage(ctx: SendCtx, p: SendPayload) {
  const pass = ctx.decrypt(ctx.mailbox.encryptedAppPassword);
  const result = await sendMail({
    host: ctx.mailbox.smtpHost, port: ctx.mailbox.smtpPort,
    user: ctx.mailbox.email, pass,
    from: p.from, to: p.to, cc: p.cc, subject: p.subject,
    text: p.text, html: p.html, attachments: p.attachments,
  });
  // Append to remote Sent (best effort)
  try {
    const rfc822 = Buffer.from(
      `From: ${p.from}\r\nTo: ${p.to.join(", ")}\r\nSubject: ${p.subject}\r\n\r\n${p.text ?? ""}`,
    );
    await appendToSent(
      { host: ctx.mailbox.imapHost, port: ctx.mailbox.imapPort, user: ctx.mailbox.email, pass },
      rfc822,
    );
  } catch { /* ignore */ }
  return { messageId: result.messageId };
}
```

- [ ] **Step 3: Run, pass**

- [ ] **Step 4: Commit**

```bash
git add . && git commit -m "feat(api): send service"
```

---

## Task 10: Messages routes — list/get/draft/move/delete/restore

**Files:**
- Create: `apps/api/src/services/search.ts`
- Create: `apps/api/src/routes/messages.ts`
- Create: `apps/api/test/integration/messages.test.ts`
- Modify: `apps/api/src/server.ts`

- [ ] **Step 1: Search query builder + unit test**

`services/search.ts`:
```ts
import type { Prisma } from "@crm/db";

export type Filters = {
  folderId?: string;
  mailboxId?: string;
  fromAddr?: string;
  dateFrom?: Date;
  dateTo?: Date;
  status?: "read" | "unread" | "all";
  q?: string;
};

export function buildWhere(f: Filters): Prisma.MessageWhereInput {
  const w: Prisma.MessageWhereInput = { deletedAt: null };
  if (f.folderId) w.folderId = f.folderId;
  if (f.mailboxId) w.mailboxId = f.mailboxId;
  if (f.fromAddr) w.fromAddr = { contains: f.fromAddr, mode: "insensitive" };
  if (f.dateFrom || f.dateTo) w.receivedAt = { gte: f.dateFrom, lte: f.dateTo };
  if (f.status === "read") w.isRead = true;
  if (f.status === "unread") w.isRead = false;
  return w;
}
```
Unit test:
```ts
import { describe, it, expect } from "vitest";
import { buildWhere } from "../../src/services/search.js";
describe("buildWhere", () => {
  it("filters unread", () => {
    expect(buildWhere({ status: "unread" }).isRead).toBe(false);
  });
  it("ignores deleted", () => {
    expect(buildWhere({}).deletedAt).toBeNull();
  });
});
```

- [ ] **Step 2: messages.ts route**

```ts
import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { prisma } from "@crm/db";
import { requireUser } from "../auth.js";
import { NotFound, BadRequest } from "../errors.js";
import { buildWhere } from "../services/search.js";

const ListQuery = z.object({
  folderId: z.string().optional(),
  mailboxId: z.string().optional(),
  q: z.string().optional(),
  from: z.string().optional(),
  dateFrom: z.coerce.date().optional(),
  dateTo: z.coerce.date().optional(),
  status: z.enum(["read", "unread", "all"]).optional(),
  cursor: z.string().optional(),
  limit: z.coerce.number().int().min(1).max(100).default(50),
});

const Create = z.object({
  mailboxId: z.string(),
  to: z.array(z.string().email()).default([]),
  cc: z.array(z.string().email()).default([]),
  subject: z.string().default(""),
  bodyText: z.string().optional(),
  bodyHtml: z.string().optional(),
});

const Patch = z.object({
  to: z.array(z.string().email()).optional(),
  cc: z.array(z.string().email()).optional(),
  subject: z.string().optional(),
  bodyText: z.string().optional(),
  bodyHtml: z.string().optional(),
  isRead: z.boolean().optional(),
  isStarred: z.boolean().optional(),
  folderId: z.string().optional(),
});

async function getOrCreateDraftsFolder(mailboxId: string) {
  const f = await prisma.folder.findFirst({ where: { mailboxId, kind: "drafts" } });
  if (f) return f;
  return prisma.folder.create({ data: { mailboxId, name: "Drafts", kind: "drafts" } });
}
async function getTrashFolder(mailboxId: string) {
  const f = await prisma.folder.findFirst({ where: { mailboxId, kind: "trash" } });
  if (f) return f;
  return prisma.folder.create({ data: { mailboxId, name: "Trash", kind: "trash" } });
}

export async function messageRoutes(app: FastifyInstance) {
  app.get("/messages", { preHandler: requireUser() }, async (req) => {
    const q = ListQuery.parse(req.query);
    const where = buildWhere({
      folderId: q.folderId, mailboxId: q.mailboxId,
      fromAddr: q.from, dateFrom: q.dateFrom, dateTo: q.dateTo, status: q.status,
    });
    if (q.q) {
      // raw FTS via $queryRaw fallback
      const rows = await prisma.$queryRawUnsafe<any[]>(
        `SELECT id FROM "Message" WHERE "fts" @@ plainto_tsquery('simple', $1) AND "deletedAt" IS NULL LIMIT ${q.limit}`,
        q.q,
      );
      const ids = rows.map(r => r.id);
      return prisma.message.findMany({ where: { id: { in: ids } }, orderBy: { receivedAt: "desc" } });
    }
    return prisma.message.findMany({
      where, orderBy: { receivedAt: "desc" }, take: q.limit,
      ...(q.cursor ? { cursor: { id: q.cursor }, skip: 1 } : {}),
    });
  });

  app.get("/messages/:id", { preHandler: requireUser() }, async (req) => {
    const id = (req.params as any).id;
    const m = await prisma.message.findUnique({ where: { id }, include: { attachments: true } });
    if (!m) throw new NotFound();
    if (!m.isRead) await prisma.message.update({ where: { id }, data: { isRead: true } });
    return m;
  });

  app.post("/messages", { preHandler: requireUser() }, async (req) => {
    const body = Create.parse(req.body);
    const drafts = await getOrCreateDraftsFolder(body.mailboxId);
    return prisma.message.create({
      data: {
        mailboxId: body.mailboxId, folderId: drafts.id, isDraft: true,
        fromAddr: "", toAddrs: body.to, ccAddrs: body.cc,
        subject: body.subject, bodyText: body.bodyText, bodyHtml: body.bodyHtml,
      },
    });
  });

  app.patch("/messages/:id", { preHandler: requireUser() }, async (req) => {
    const id = (req.params as any).id;
    const body = Patch.parse(req.body);
    const data: any = { ...body };
    if (body.to) data.toAddrs = body.to;
    if (body.cc) data.ccAddrs = body.cc;
    delete data.to; delete data.cc;
    return prisma.message.update({ where: { id }, data });
  });

  app.delete("/messages/:id", { preHandler: requireUser() }, async (req, reply) => {
    const id = (req.params as any).id;
    const m = await prisma.message.findUnique({ where: { id } });
    if (!m) throw new NotFound();
    const trash = await getTrashFolder(m.mailboxId);
    await prisma.message.update({ where: { id }, data: { deletedAt: new Date(), folderId: trash.id } });
    return reply.status(204).send();
  });

  app.post("/messages/:id/restore", { preHandler: requireUser() }, async (req) => {
    const id = (req.params as any).id;
    const m = await prisma.message.findUnique({ where: { id } });
    if (!m || !m.deletedAt) throw new BadRequest("not in trash");
    const inbox = await prisma.folder.findFirst({ where: { mailboxId: m.mailboxId, kind: "inbox" } });
    if (!inbox) throw new NotFound("inbox missing");
    return prisma.message.update({ where: { id }, data: { deletedAt: null, folderId: inbox.id } });
  });
}
```

- [ ] **Step 3: Integration test (uses seeded mailbox row directly, no IMAP)**

`messages.test.ts` — beforeAll like auth.test.ts, plus seed a mailbox row + inbox folder + sample messages, then exercise list/get/draft create/patch/delete/restore. Use `app.inject` for each call. (Write the actual code at execution time — pattern matches `auth.test.ts` and `folders.test.ts`.)

- [ ] **Step 4: Register, run, pass, commit**

```ts
// server.ts
import { messageRoutes } from "./routes/messages.js";
await app.register(messageRoutes);
```
```bash
pnpm --filter @crm/api test messages
git add . && git commit -m "feat(api): message routes (list/get/draft/move/delete/restore)"
```

---

## Task 11: Attachments upload + download

**Files:**
- Create: `apps/api/src/routes/attachments.ts`
- Modify: `apps/api/src/server.ts`
- Add to: `apps/api/test/integration/messages.test.ts`

- [ ] **Step 1: Implement attachments route**

```ts
import type { FastifyInstance } from "fastify";
import { mkdir, writeFile } from "node:fs/promises";
import { createReadStream } from "node:fs";
import { join, dirname } from "node:path";
import { prisma } from "@crm/db";
import { requireUser } from "../auth.js";
import { loadConfig } from "../config.js";
import { NotFound, BadRequest } from "../errors.js";

export async function attachmentRoutes(app: FastifyInstance) {
  const cfg = loadConfig();

  app.post("/messages/:id/attachments", { preHandler: requireUser() }, async (req) => {
    const id = (req.params as any).id;
    const m = await prisma.message.findUnique({ where: { id } });
    if (!m) throw new NotFound();
    const file = await req.file();
    if (!file) throw new BadRequest("no file");
    const buf = await file.toBuffer();
    if (buf.length > 25 * 1024 * 1024) throw new BadRequest("file > 25MB; use a cloud link");
    const dir = join(cfg.attachmentDir, m.mailboxId, m.id);
    await mkdir(dir, { recursive: true });
    const path = join(dir, file.filename);
    await writeFile(path, buf);
    return prisma.attachment.create({
      data: { messageId: id, filename: file.filename, mime: file.mimetype, size: buf.length, storagePath: path },
    });
  });

  app.get("/attachments/:id", { preHandler: requireUser() }, async (req, reply) => {
    const id = (req.params as any).id;
    const a = await prisma.attachment.findUnique({ where: { id } });
    if (!a) throw new NotFound();
    reply.header("content-type", a.mime);
    reply.header("content-disposition", `attachment; filename="${a.filename}"`);
    return reply.send(createReadStream(a.storagePath));
  });
}
```

- [ ] **Step 2: Test**

In `messages.test.ts`, add: upload a small `Buffer` via multipart payload, assert 200 + size, then GET `/attachments/:id` and assert body matches.

- [ ] **Step 3: Register, run, pass, commit**

```bash
git add . && git commit -m "feat(api): attachment upload/download"
```

---

## Task 12: Queue + scheduled send route + worker

**Files:**
- Create: `apps/api/src/queue.ts`
- Create: `apps/api/src/workers/scheduled-send.ts`
- Modify: `apps/api/src/routes/messages.ts` (add `/messages/:id/send`)
- Create: `apps/api/test/integration/scheduled-send.test.ts`

- [ ] **Step 1: queue.ts**

```ts
import { Queue, Worker, QueueEvents } from "bullmq";
import { loadConfig } from "./config.js";

const cfg = loadConfig();
const connection = { url: cfg.redisUrl };

export const sendQueue = new Queue("scheduled-send", { connection });
export const cleanupQueue = new Queue("trash-cleanup", { connection });

export function makeSendWorker(processor: (jobData: any) => Promise<void>) {
  return new Worker("scheduled-send", async job => processor(job.data), { connection });
}
export function makeCleanupWorker(processor: () => Promise<void>) {
  return new Worker("trash-cleanup", async () => processor(), { connection });
}
```

- [ ] **Step 2: Add /messages/:id/send route**

In `routes/messages.ts`:
```ts
import { sendQueue } from "../queue.js";
import { sendMessage } from "../services/send.js";
import { decrypt } from "../crypto.js";

const SendBody = z.object({ sendAt: z.coerce.date().optional() });

app.post("/messages/:id/send", { preHandler: requireUser() }, async (req) => {
  const id = (req.params as any).id;
  const body = SendBody.parse(req.body ?? {});
  const m = await prisma.message.findUnique({ where: { id }, include: { mailbox: true, attachments: true } });
  if (!m) throw new NotFound();
  const user = (req as any).user;

  if (body.sendAt && body.sendAt > new Date()) {
    const sched = await prisma.scheduledSend.create({
      data: {
        userId: user.id, mailboxId: m.mailboxId,
        payload: { messageId: id }, sendAt: body.sendAt, status: "pending",
      },
    });
    const delay = body.sendAt.getTime() - Date.now();
    const job = await sendQueue.add("send", { scheduledId: sched.id }, { delay });
    await prisma.scheduledSend.update({ where: { id: sched.id }, data: { jobId: job.id } });
    return { scheduled: true, id: sched.id };
  }

  const result = await sendMessage(
    { mailbox: m.mailbox, decrypt: (b) => decrypt(b, m.mailbox.email) },
    {
      from: m.mailbox.email, to: m.toAddrs, cc: m.ccAddrs,
      subject: m.subject, text: m.bodyText ?? "", html: m.bodyHtml ?? undefined,
    },
  );
  const sent = await prisma.folder.findFirst({ where: { mailboxId: m.mailboxId, kind: "sent" } })
    ?? await prisma.folder.create({ data: { mailboxId: m.mailboxId, name: "Sent", kind: "sent" } });
  await prisma.message.update({
    where: { id },
    data: { isDraft: false, sentAt: new Date(), folderId: sent.id, messageId: result.messageId, fromAddr: m.mailbox.email },
  });
  return { sent: true };
});
```

- [ ] **Step 3: workers/scheduled-send.ts**

```ts
import { prisma } from "@crm/db";
import { sendMessage } from "../services/send.js";
import { decrypt } from "../crypto.js";
import { makeSendWorker } from "../queue.js";

export function startScheduledSendWorker() {
  return makeSendWorker(async ({ scheduledId }) => {
    const sched = await prisma.scheduledSend.findUnique({ where: { id: scheduledId } });
    if (!sched || sched.status !== "pending") return;
    const payload = sched.payload as { messageId: string };
    const m = await prisma.message.findUnique({ where: { id: payload.messageId }, include: { mailbox: true } });
    if (!m) return;
    try {
      const r = await sendMessage(
        { mailbox: m.mailbox, decrypt: (b) => decrypt(b, m.mailbox.email) },
        { from: m.mailbox.email, to: m.toAddrs, cc: m.ccAddrs, subject: m.subject, text: m.bodyText ?? "" },
      );
      const sent = await prisma.folder.findFirst({ where: { mailboxId: m.mailboxId, kind: "sent" } })
        ?? await prisma.folder.create({ data: { mailboxId: m.mailboxId, name: "Sent", kind: "sent" } });
      await prisma.$transaction([
        prisma.message.update({ where: { id: m.id }, data: { isDraft: false, sentAt: new Date(), folderId: sent.id, messageId: r.messageId } }),
        prisma.scheduledSend.update({ where: { id: sched.id }, data: { status: "sent" } }),
      ]);
    } catch (e) {
      await prisma.scheduledSend.update({ where: { id: sched.id }, data: { status: "failed" } });
      throw e;
    }
  });
}
```

- [ ] **Step 4: Integration test (mock send service)**

```ts
// scheduled-send.test.ts
// vi.mock("../../src/services/send.js", () => ({ sendMessage: vi.fn().mockResolvedValue({ messageId: "<m@x>" }) }));
// 1. login, create draft
// 2. POST /messages/:id/send { sendAt: Date.now()+1500 } → 200, scheduled:true
// 3. start worker
// 4. wait 3s, assert ScheduledSend.status === "sent" and Message.sentAt set
```

- [ ] **Step 5: Run, pass, commit**

```bash
pnpm --filter @crm/api test scheduled
git add . && git commit -m "feat(api): scheduled send via bullmq"
```

---

## Task 13: Trash cleanup worker (TDD)

**Files:**
- Create: `apps/api/src/workers/trash-cleanup.ts`
- Create: `apps/api/test/integration/trash-cleanup.test.ts`

- [ ] **Step 1: Failing test**

```ts
// 1. seed mailbox + trash folder + 2 messages: one deletedAt = 31 days ago, one deletedAt = 5 days ago
// 2. write a fake attachment file under tmp dir; reference from old message
// 3. call runCleanup()
// 4. assert old message gone, attachment row gone, file unlinked, recent message still present
```

- [ ] **Step 2: Implement trash-cleanup.ts**

```ts
import { unlink } from "node:fs/promises";
import { prisma } from "@crm/db";
import { makeCleanupWorker, cleanupQueue } from "../queue.js";

const THIRTY_DAYS_MS = 30 * 24 * 60 * 60 * 1000;

export async function runCleanup() {
  const cutoff = new Date(Date.now() - THIRTY_DAYS_MS);
  const old = await prisma.message.findMany({
    where: { deletedAt: { lt: cutoff } },
    include: { attachments: true },
  });
  for (const m of old) {
    for (const a of m.attachments) {
      try { await unlink(a.storagePath); } catch {}
    }
    await prisma.message.delete({ where: { id: m.id } });
  }
  return old.length;
}

export function startTrashCleanupWorker() {
  cleanupQueue.add("daily", {}, { repeat: { pattern: "0 3 * * *" }, jobId: "trash-daily" });
  return makeCleanupWorker(runCleanup);
}
```

- [ ] **Step 3: Run, pass, commit**

```bash
pnpm --filter @crm/api test trash
git add . && git commit -m "feat(api): trash cleanup worker"
```

---

## Task 14: IMAP sync worker

**Files:**
- Create: `apps/api/src/workers/sync.ts`
- Create: `apps/api/test/helpers/greenmail.ts`
- Create: `apps/api/test/integration/send-receive.test.ts`

- [ ] **Step 1: greenmail helper**

```ts
import { GenericContainer, StartedTestContainer } from "testcontainers";

export type Greenmail = {
  c: StartedTestContainer;
  imapHost: string; imapPort: number;
  smtpHost: string; smtpPort: number;
  stop: () => Promise<void>;
};

export async function startGreenmail(): Promise<Greenmail> {
  const c = await new GenericContainer("greenmail/standalone:2.1.0")
    .withExposedPorts(3025, 3143, 3993, 3465)
    .withEnvironment({ GREENMAIL_OPTS: "-Dgreenmail.setup.test.all -Dgreenmail.users=alice:pw@local,bob:pw@local -Dgreenmail.auth.disabled" })
    .start();
  return {
    c,
    smtpHost: c.getHost(), smtpPort: c.getMappedPort(3025),
    imapHost: c.getHost(), imapPort: c.getMappedPort(3143),
    stop: () => c.stop(),
  };
}
```

- [ ] **Step 2: workers/sync.ts**

```ts
import { ImapFlow } from "imapflow";
import { mkdir, writeFile } from "node:fs/promises";
import { join } from "node:path";
import { prisma } from "@crm/db";
import { parseRaw } from "@crm/mail/dist/parse.js"; // adjust path if not built; see Step 3
import { decrypt } from "../crypto.js";
import { loadConfig } from "../config.js";

const cfg = loadConfig();
const clients = new Map<string, ImapFlow>();

export async function startSyncFor(mailboxId: string) {
  const m = await prisma.mailbox.findUnique({ where: { id: mailboxId } });
  if (!m || !m.enabled) return;
  if (clients.has(mailboxId)) return;

  const pass = decrypt(m.encryptedAppPassword, m.email);
  const client = new ImapFlow({
    host: m.imapHost, port: m.imapPort, secure: m.imapPort === 993,
    auth: { user: m.email, pass }, logger: false,
  });
  clients.set(mailboxId, client);
  await client.connect();
  await client.mailboxOpen("INBOX");

  const inbox = await prisma.folder.findFirst({ where: { mailboxId, kind: "inbox" } })
    ?? await prisma.folder.create({ data: { mailboxId, name: "INBOX", kind: "inbox" } });

  const state = await prisma.syncState.upsert({
    where: { mailboxId }, create: { mailboxId, lastUid: 0 }, update: {},
  });

  async function fetchSince(uid: number) {
    let max = uid;
    for await (const msg of client.fetch({ uid: `${uid + 1}:*` }, { source: true, uid: true, envelope: true, internalDate: true })) {
      const parsed = await parseRaw(msg.source!);
      const created = await prisma.message.create({
        data: {
          mailboxId, folderId: inbox.id, imapUid: msg.uid,
          messageId: parsed.messageId, fromAddr: parsed.from,
          toAddrs: parsed.to, ccAddrs: parsed.cc, subject: parsed.subject,
          bodyText: parsed.text, bodyHtml: parsed.html,
          receivedAt: parsed.date ?? msg.internalDate,
        },
      });
      for (const a of parsed.attachments) {
        const dir = join(cfg.attachmentDir, mailboxId, created.id);
        await mkdir(dir, { recursive: true });
        const path = join(dir, a.filename);
        await writeFile(path, a.content);
        await prisma.attachment.create({
          data: { messageId: created.id, filename: a.filename, mime: a.mime, size: a.size, storagePath: path },
        });
      }
      if (msg.uid > max) max = msg.uid;
    }
    if (max !== uid) await prisma.syncState.update({ where: { mailboxId }, data: { lastUid: max } });
  }

  await fetchSince(state.lastUid);

  client.on("exists", async () => {
    const s = await prisma.syncState.findUnique({ where: { mailboxId } });
    await fetchSince(s?.lastUid ?? 0);
  });

  await client.idle().catch(() => {});
}

export async function stopAllSync() {
  for (const c of clients.values()) await c.logout().catch(() => {});
  clients.clear();
}
```

- [ ] **Step 3: Resolve mail package import**

In `packages/mail/package.json`, add `"main": "src/index.ts"` and create `packages/mail/src/index.ts`:
```ts
export * from "./parse.js";
export * from "./smtp.js";
export * from "./imap.js";
```
Update `workers/sync.ts` import to `from "@crm/mail"`.

- [ ] **Step 4: send-receive integration test**

```ts
// 1. startStack() + startGreenmail()
// 2. seed mailbox row pointing at greenmail (use crypto.encrypt to store "pw")
// 3. send a raw email via nodemailer to alice@local through greenmail SMTP
// 4. await startSyncFor(mailbox.id)
// 5. login, GET /messages?folderId=<inbox> → assert 1 message with right subject
// 6. afterAll: stopAllSync(), greenmail.stop(), stack.stop()
```

- [ ] **Step 5: Run, pass, commit**

```bash
pnpm --filter @crm/api test send-receive
git add . && git commit -m "feat(api): imap sync worker + e2e send-receive test"
```

---

## Task 15: Workers entrypoint + seed CLIs

**Files:**
- Create: `apps/api/src/workers.ts`
- Create: `apps/api/src/cli/seed-owner.ts`
- Create: `apps/api/src/cli/seed-mailbox.ts`

- [ ] **Step 1: workers.ts**

```ts
import { loadConfig } from "./config.js";
import { setKey } from "./crypto.js";
import { prisma } from "@crm/db";
import { startSyncFor } from "./workers/sync.js";
import { startScheduledSendWorker } from "./workers/scheduled-send.js";
import { startTrashCleanupWorker } from "./workers/trash-cleanup.js";

const cfg = loadConfig();
setKey(cfg.encKey);

const mailboxes = await prisma.mailbox.findMany({ where: { enabled: true } });
for (const m of mailboxes) await startSyncFor(m.id);
startScheduledSendWorker();
startTrashCleanupWorker();

console.log(`workers up: ${mailboxes.length} mailboxes`);
```

- [ ] **Step 2: seed-owner.ts**

```ts
import { prisma } from "@crm/db";
import { hashPassword } from "../auth.js";

const args = Object.fromEntries(process.argv.slice(2).map(a => {
  const [k, v] = a.replace(/^--/, "").split("=");
  return [k, v];
}));
if (!args.email || !args.password) {
  console.error("usage: seed-owner --email=... --password=...");
  process.exit(1);
}
const u = await prisma.user.upsert({
  where: { email: args.email },
  update: {},
  create: { email: args.email, passwordHash: await hashPassword(args.password), name: args.name ?? "Owner", role: "owner" },
});
console.log("ok", u.id);
```

- [ ] **Step 3: seed-mailbox.ts**

```ts
import { prisma } from "@crm/db";
import { loadConfig } from "../config.js";
import { setKey, encrypt } from "../crypto.js";

setKey(loadConfig().encKey);
const args = Object.fromEntries(process.argv.slice(2).map(a => {
  const [k, v] = a.replace(/^--/, "").split("=");
  return [k, v];
}));
if (!args.email || !args["app-password"] || !args.name) {
  console.error("usage: seed-mailbox --email=... --app-password=... --name=...");
  process.exit(1);
}
const enc = encrypt(args["app-password"], args.email);
const m = await prisma.mailbox.upsert({
  where: { email: args.email },
  update: { encryptedAppPassword: enc, displayName: args.name, enabled: true },
  create: { email: args.email, displayName: args.name, encryptedAppPassword: enc },
});
await prisma.folder.upsert({
  where: { id: `${m.id}-inbox-stub` }, // never matches; create branch always runs
  update: {},
  create: { mailboxId: m.id, name: "INBOX", kind: "inbox" },
}).catch(() => {});
console.log("ok", m.id);
```

Add to `apps/api/package.json` scripts:
```json
"seed:owner": "tsx src/cli/seed-owner.ts",
"seed:mailbox": "tsx src/cli/seed-mailbox.ts"
```

- [ ] **Step 4: Commit**

```bash
git add . && git commit -m "feat(api): workers entrypoint + seed clis"
```

---

## Task 16: Deploy ops (systemd + nginx)

**Files:**
- Create: `ops/systemd/crm-api.service`
- Create: `ops/systemd/crm-sync.service`
- Create: `ops/nginx/crm.eg.je.conf`
- Create: `ops/README.md`

- [ ] **Step 1: crm-api.service**

```ini
[Unit]
Description=crm.eg.je API
After=network.target postgresql.service redis-server.service

[Service]
Type=simple
User=crm
WorkingDirectory=/opt/crm.eg.je/apps/api
EnvironmentFile=/etc/crm/secrets.env
Environment=NODE_ENV=production
ExecStartPre=/usr/bin/pnpm --filter @crm/db exec prisma migrate deploy
ExecStart=/usr/bin/node dist/server.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 2: crm-sync.service**

```ini
[Unit]
Description=crm.eg.je sync + scheduled workers
After=network.target postgresql.service redis-server.service crm-api.service

[Service]
Type=simple
User=crm
WorkingDirectory=/opt/crm.eg.je/apps/api
EnvironmentFile=/etc/crm/secrets.env
Environment=NODE_ENV=production
ExecStart=/usr/bin/node dist/workers.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 3: nginx vhost**

```nginx
server {
    listen 80;
    server_name crm.eg.je;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name crm.eg.je;

    ssl_certificate     /etc/letsencrypt/live/crm.eg.je/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/crm.eg.je/privkey.pem;

    client_max_body_size 30M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto https;
    }
}
```

- [ ] **Step 4: ops/README.md (deploy runbook)**

```md
# Deploy runbook (subsystem 1)

## One-time

1. `sudo useradd -r -m -d /var/lib/crm crm`
2. `sudo mkdir -p /etc/crm /var/lib/crm/attachments && sudo chown -R crm:crm /var/lib/crm`
3. Create `/etc/crm/secrets.env` (mode 600, owner crm:crm):
   ```
   DATABASE_URL=postgresql://crm:STRONG@localhost:5432/crm
   REDIS_URL=redis://localhost:6379
   CRM_ENC_KEY=$(openssl rand -base64 32)
   SESSION_SECRET=$(openssl rand -base64 32)
   ATTACHMENT_DIR=/var/lib/crm/attachments
   PORT=3000
   NODE_ENV=production
   ```
4. `sudo -u postgres createuser crm --pwprompt && sudo -u postgres createdb crm -O crm`
5. `sudo apt install redis-server && sudo systemctl enable --now redis-server`
6. `cd /opt/crm.eg.je && pnpm install && pnpm -r build`
7. Install units:
   ```
   sudo cp ops/systemd/*.service /etc/systemd/system/
   sudo systemctl daemon-reload
   sudo systemctl enable --now crm-api crm-sync
   ```
8. nginx + TLS:
   ```
   sudo cp ops/nginx/crm.eg.je.conf /etc/nginx/sites-available/
   sudo ln -s /etc/nginx/sites-available/crm.eg.je.conf /etc/nginx/sites-enabled/
   sudo certbot --nginx -d crm.eg.je
   sudo nginx -t && sudo systemctl reload nginx
   ```
9. Seed owner: `sudo -u crm pnpm --filter @crm/api seed:owner --email=you@x --password=...`
10. Seed each mailbox via `seed:mailbox`.

## Health check

`curl https://crm.eg.je/health` → `{"ok":true}`.

## Logs

`journalctl -u crm-api -f` and `journalctl -u crm-sync -f`.
```

- [ ] **Step 5: Commit**

```bash
git add . && git commit -m "ops: systemd units, nginx vhost, deploy runbook"
```

---

## Task 17: CI gate — full test run

- [ ] **Step 1: Run everything**

```bash
cd /opt/crm.eg.je
pnpm -r build
pnpm -r test
```
Expected: all unit + integration suites green. testcontainers will pull images on first run.

- [ ] **Step 2: Tag the milestone**

```bash
git tag mail-core-v1
```

---

## Spec Coverage Check

| Spec section | Task |
|---|---|
| §2 Tech stack | 1, 2, 3 |
| §3 Repo layout | 1, 2, 3, 8 |
| §4 Data model | 2 |
| §5 Secret storage | 3 |
| §6 IMAP sync worker | 14 |
| §7 SMTP send path | 9, 12 |
| §8 Trash lifecycle | 10, 13 |
| §9 REST API | 6, 7, 10, 11, 12 |
| §10 Auth | 6 |
| §11 Errors & validation | 5, plus zod throughout |
| §12 Testing strategy | 6, 10, 12, 13, 14, 17 |
| §13 Deployment | 16 |
| §14 Seed data | 15 |
