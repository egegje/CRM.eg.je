{
  "name": "crm.eg.je",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:api": "pnpm --filter @crm/api dev",
    "dev:workers": "pnpm --filter @crm/api workers"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
